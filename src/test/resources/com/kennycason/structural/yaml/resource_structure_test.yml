---
tests:
  -
    mode: structure
    data:
      resource: /com/kennycason/structural/json/person_sample_response.json

    expects:
      - name
      - age
      - job:
          id
          title

